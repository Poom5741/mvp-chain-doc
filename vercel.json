{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build", "buildCommand": "npm run build:vercel" }
    }
  ],
  "buildCommand": "npm run build:vercel",
  "env": {
    "PUPPETEER_SKIP_DOWNLOAD": "1"
  }
}